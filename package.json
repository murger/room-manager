{
	"name": "room-manager",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"clean": "rimraf ./dist ./.cache",
		"watch": "parcel ./src/index.html",
		"dev": "npm run clean && cross-env mockup=true npm run watch",
		"stage": "npm run clean && npm run watch",
		"build": "npm run clean && parcel build ./src/index.html --no-source-maps"
	},
	"author": "Gürhan Mermer <g.mermer@emakina.com.tr>",
	"license": "MIT",
	"dependencies": {
		"babel-preset-env": "^1.7.0",
		"babel-preset-react": "^6.24.1",
		"node-sass": "^4.9.0",
		"parcel-bundler": "^1.9.3",
		"react": "^16.4.1",
		"react-dom": "^16.4.1"
	},
	"devDependencies": {
		"cross-env": "^5.2.0",
		"rimraf": "^2.6.2"
	}
}
